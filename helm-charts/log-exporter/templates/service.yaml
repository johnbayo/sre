apiVersion: v1
kind: Service
metadata:
  name: {{ include "log-exporter.fullname" . }}
  labels:
    {{- include "log-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "log-exporter.selectorLabels" . | nindent 4 }}
