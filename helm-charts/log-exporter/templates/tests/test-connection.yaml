apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "log-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "log-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "log-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
