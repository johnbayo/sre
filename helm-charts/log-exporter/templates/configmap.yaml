{{- if .Values.configmap }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmap.name }}
  labels:
    {{- include "log-exporter.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.configmap | nindent 2 }}
{{- end }}
